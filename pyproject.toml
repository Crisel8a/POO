[project]
name = "poo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Crisel", email = "Crisel" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
poo = "poo:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
